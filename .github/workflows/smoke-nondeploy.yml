name: Non-deploy Smoke Tests

on:
  workflow_dispatch:

concurrency:
  group: smoke-nondeploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  nondeploy-smoke:
    name: Non-deploy smoke (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    env:
      CI: '1'
      FORCE_COLOR: '0'
      TZ: 'UTC'
      LC_ALL: 'C'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Enable Corepack & setup pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.13.1 --activate
      - name: Approve native build scripts
        run: pnpm approve-builds @tailwindcss/oxide esbuild sharp workerd

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Sidecar handshake smoke (Windows)
        if: ${{ runner.os == 'Windows' }}
        shell: pwsh
        run: ./packages/cli/scripts/smoke/smoke-sidecar.ps1

      - name: Packaging helpers smoke (Windows)
        if: ${{ runner.os == 'Windows' }}
        shell: pwsh
        run: ./packages/cli/scripts/smoke/smoke-packaging.ps1

      - name: Sidecar handshake smoke (Linux)
        if: ${{ runner.os == 'Linux' }}
        run: bash packages/cli/scripts/smoke/smoke-sidecar.sh

      - name: Packaging helpers smoke (Linux)
        if: ${{ runner.os == 'Linux' }}
        run: bash packages/cli/scripts/smoke/smoke-packaging.sh

      - name: Upload smoke artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-nondeploy-${{ matrix.os }}
          path: |
            ./.artifacts/**/*
          if-no-files-found: ignore
