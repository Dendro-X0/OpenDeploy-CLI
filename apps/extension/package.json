{
  "name": "opendeploy-vscode",
  "displayName": "OpenDeploy",
  "description": "Plan and deploy web apps from VSCode using OpenDeploy.",
  "version": "1.2.1",
  "publisher": "opendeploy",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Dendro-X0/OpenDeploy-CLI"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": ["Other"],
  "main": "./dist/extension.js",
  "files": [
    "dist/**",
    "package.json",
    "README.md",
    "LICENSE"
  ],
  "contributes": {
    "commands": [
      { "command": "opendeploy.panel", "title": "OpenDeploy: Control Panel" },
      { "command": "opendeploy.plan", "title": "OpenDeploy: Plan" },
      { "command": "opendeploy.deploy", "title": "OpenDeploy: Deploy" },
      { "command": "opendeploy.detect", "title": "OpenDeploy: Detect App" },
      { "command": "opendeploy.summary", "title": "OpenDeploy: Summary" },
      { "command": "opendeploy.toggleJson", "title": "OpenDeploy: Toggle JSON View" },
      { "command": "opendeploy.checkVercelAuth", "title": "OpenDeploy: Check Vercel Auth" },
      { "command": "opendeploy.checkCloudflareAuth", "title": "OpenDeploy: Check Cloudflare Auth" },
      { "command": "opendeploy.generateGhPagesWorkflow", "title": "OpenDeploy: Generate GH Pages Workflow" },
      { "command": "opendeploy.doctor", "title": "OpenDeploy: Doctor" },
      { "command": "opendeploy.openVercel", "title": "OpenDeploy: Open Vercel Dashboard" },
      { "command": "opendeploy.openCloudflare", "title": "OpenDeploy: Open Cloudflare Dashboard" },
      { "command": "opendeploy.openGithub", "title": "OpenDeploy: Open GitHub Actions" }
      ,{ "command": "opendeploy.followLogs", "title": "OpenDeploy: Follow Logs" }
      ,{ "command": "opendeploy.scan", "title": "OpenDeploy: Scan Repo" }
      ,{ "command": "opendeploy.scanStrict", "title": "OpenDeploy: Scan Repo (Strict)" }
      ,{ "command": "opendeploy.vercelSetAlias", "title": "OpenDeploy: Vercel — Set Alias" }
      ,{ "command": "opendeploy.vercelRollback", "title": "OpenDeploy: Vercel — Rollback" }
      ,{ "command": "opendeploy.onboarding", "title": "OpenDeploy: Onboarding" }
    ],
    "keybindings": [
      {
        "command": "opendeploy.panel",
        "key": "ctrl+alt+u",
        "mac": "cmd+alt+u",
        "when": "workbenchState != 'empty'"
      },
      {
        "command": "opendeploy.toggleJson",
        "key": "ctrl+alt+j",
        "mac": "cmd+alt+j",
        "when": "workbenchState != 'empty'"
      },
      {
        "command": "opendeploy.summary",
        "key": "ctrl+alt+o",
        "mac": "cmd+alt+o",
        "when": "workbenchState != 'empty'"
      }
      ,{
        "command": "opendeploy.plan",
        "key": "ctrl+alt+p",
        "mac": "cmd+alt+p",
        "when": "workbenchState != 'empty'"
      }
      ,{
        "command": "opendeploy.openVercel",
        "key": "ctrl+alt+v",
        "mac": "cmd+alt+v",
        "when": "workbenchState != 'empty'"
      }
      ,{
        "command": "opendeploy.openCloudflare",
        "key": "ctrl+alt+c",
        "mac": "cmd+alt+c",
        "when": "workbenchState != 'empty'"
      }
      ,{
        "command": "opendeploy.openGithub",
        "key": "ctrl+alt+g",
        "mac": "cmd+alt+g",
        "when": "workbenchState != 'empty'"
      }
      ,{
        "command": "opendeploy.followLogs",
        "key": "ctrl+alt+l",
        "mac": "cmd+alt+l",
        "when": "workbenchState != 'empty'"
      }
      ,{
        "command": "opendeploy.vercelSetAlias",
        "key": "ctrl+alt+a",
        "mac": "cmd+alt+a",
        "when": "workbenchState != 'empty'"
      }
      ,{
        "command": "opendeploy.vercelRollback",
        "key": "ctrl+alt+r",
        "mac": "cmd+alt+r",
        "when": "workbenchState != 'empty'"
      }
      ,{
        "command": "opendeploy.onboarding",
        "key": "ctrl+alt+w",
        "mac": "cmd+alt+w",
        "when": "workbenchState != 'empty'"
      }
      ,{
        "command": "opendeploy.scanStrict",
        "key": "ctrl+alt+shift+s",
        "mac": "cmd+alt+shift+s",
        "when": "workbenchState != 'empty'"
      }
      ,{
        "command": "opendeploy.scan",
        "key": "ctrl+alt+s",
        "mac": "cmd+alt+s",
        "when": "workbenchState != 'empty'"
      }
    ],
    "configuration": {
      "title": "OpenDeploy",
      "properties": {
        "opendeploy.runner": {
          "type": "string",
          "enum": ["npm", "docker"],
          "default": "npm",
          "markdownDescription": "Backend used to run OpenDeploy (npm via npx, or Docker image)."
        },
        "opendeploy.npmBinary": {
          "type": "string",
          "default": "npx opendeploy@latest",
          "markdownDescription": "Command used to run OpenDeploy via npm."
        },
        "opendeploy.dockerImage": {
          "type": "string",
          "default": "ghcr.io/dendro-x0/opd:latest",
          "markdownDescription": "Docker image to run when runner is 'docker'."
        },
        "opendeploy.defaultPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Default path of the app in the workspace (for monorepos)."
        },
        "opendeploy.preferJson": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Prefer JSON output rendering when available."
        }
      }
    }
  },
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "package": "vsce package",
    "publish": "vsce publish",
    "stage:pack": "node ./scripts/stage-package.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.9.0",
    "vsce": "^2.15.0"
  }
}
